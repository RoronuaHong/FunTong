# 背景图片设置指南

## 概述

项目已配置动态背景轮播系统，支持自动切换背景图片，提供优雅的视觉体验。

## 功能特性

✨ **已实现的功能**

- ✅ 自动轮播背景图片（每 8 秒切换）
- ✅ 平滑的淡入淡出过渡效果
- ✅ 缓慢放大动画，营造动态感
- ✅ 半透明毛玻璃效果，提升内容可读性
- ✅ 侧边栏和内容区域使用半透明背景
- ✅ 响应式设计，适配各种屏幕尺寸

## 放置背景图片

### 步骤 1: 准备图片文件

确保你有以下 4 张图片：
- `am.jpg`
- `dr.jpg`
- `pom.jpg`
- `wr.jpg`

**图片要求：**
- 格式: JPG/JPEG
- 推荐尺寸: 1920x1080 或更高
- 比例: 16:9 横向
- 文件大小: 建议 ≤ 500KB（优化后）

### 步骤 2: 放置图片

将图片文件复制到以下目录：

```bash
frontend/public/backgrounds/
```

**方式 1: 使用命令行**

```bash
# 从当前目录复制图片
cp am.jpg dr.jpg pom.jpg wr.jpg frontend/public/backgrounds/

# 或从其他路径复制
cp /path/to/images/*.jpg frontend/public/backgrounds/
```

**方式 2: 使用文件管理器**

1. 打开 `frontend/public/backgrounds/` 文件夹
2. 将 4 张图片拖放到此文件夹
3. 确保文件名完全匹配（区分大小写）

### 步骤 3: 验证

启动开发服务器：

```bash
cd frontend
npm run dev
```

访问 http://localhost:3000，你应该能看到：
- 背景图片自动轮播
- 每 8 秒切换一次
- 平滑的过渡效果

## 样式优化说明

### 视觉效果

1. **背景轮播**
   - 4 张图片循环播放
   - 淡入淡出过渡（1.5 秒）
   - 缓慢放大效果（8 秒内从 1.0 到 1.1 倍）

2. **半透明遮罩**
   - 渐变遮罩层，从左到右透明度变化
   - 2px 毛玻璃模糊效果
   - 提升前景内容的可读性

3. **界面元素**
   - 侧边栏: 85% 不透明度深色背景 + 10px 模糊
   - 顶部栏: 85% 不透明度白色背景 + 10px 模糊
   - 内容区: 90% 不透明度白色背景 + 10px 模糊
   - 所有元素都有轻微阴影，增强层次感

### 优化细节

- 表格和卡片使用 95% 不透明度，确保内容清晰
- 模态框使用 98% 不透明度 + 12px 模糊
- 鼠标悬停时行高亮效果
- 平滑滚动体验

## 自定义配置

### 修改轮播间隔

编辑 `frontend/src/components/BackgroundSlider.tsx`:

```typescript
// 将 8000 改为你想要的毫秒数
setInterval(() => {
  setCurrentIndex((prev) => (prev + 1) % backgrounds.length);
}, 8000); // 8 秒
```

### 修改过渡效果

编辑 `frontend/src/components/BackgroundSlider.css`:

```css
.background-slide {
  /* 修改过渡时间 */
  transition: opacity 1.5s ease-in-out; /* 改为 2s、3s 等 */
}

@keyframes zoom-in {
  /* 修改放大倍数 */
  100% {
    transform: scale(1.1); /* 改为 1.15、1.2 等 */
  }
}
```

### 修改遮罩透明度

编辑 `frontend/src/components/BackgroundSlider.css`:

```css
.background-overlay {
  background: linear-gradient(
    135deg,
    rgba(0, 0, 0, 0.3) 0%,    /* 左侧暗度 */
    rgba(0, 0, 0, 0.1) 50%,   /* 中间暗度 */
    rgba(0, 0, 0, 0.3) 100%   /* 右侧暗度 */
  );
  backdrop-filter: blur(2px); /* 模糊程度 */
}
```

### 添加更多背景图片

1. 将新图片放到 `frontend/public/backgrounds/`
2. 编辑 `frontend/src/components/BackgroundSlider.tsx`:

```typescript
const backgrounds = [
  "/backgrounds/am.jpg",
  "/backgrounds/dr.jpg",
  "/backgrounds/pom.jpg",
  "/backgrounds/wr.jpg",
  "/backgrounds/new-image.jpg", // 添加新图片
];
```

### 修改界面透明度

编辑 `frontend/src/components/MainLayout.tsx`:

```typescript
// 侧边栏透明度
background: "rgba(0, 21, 41, 0.85)", // 最后的 0.85 是透明度

// 顶部栏透明度
background: "rgba(255, 255, 255, 0.85)",

// 内容区透明度
background: "rgba(255, 255, 255, 0.9)",
```

## 图片优化

### 在线工具

- **[TinyPNG](https://tinypng.com/)** - 智能无损压缩
- **[Squoosh](https://squoosh.app/)** - Google 的图片优化工具
- **[Compressor.io](https://compressor.io/)** - 在线压缩

### 命令行工具

使用 ImageMagick:

```bash
# 安装
brew install imagemagick  # macOS
apt-get install imagemagick  # Linux

# 单张图片优化
convert input.jpg -resize 1920x1080 -quality 85 output.jpg

# 批量优化
cd /path/to/images
for img in *.jpg; do
  convert "$img" -resize 1920x1080 -quality 85 "optimized_$img"
done
```

使用 jpegoptim:

```bash
# 安装
brew install jpegoptim  # macOS

# 优化图片（保持最大质量 85%）
jpegoptim --max=85 --strip-all *.jpg
```

## 性能优化

### Next.js 图片优化

项目使用 Next.js Image 组件，自动提供：
- 懒加载
- 自动格式转换（WebP）
- 响应式图片
- 模糊占位符

### 首屏加载优化

```typescript
// 第一张图片使用 priority 属性
<Image
  src={bg}
  priority={index === 0}  // 首张图片优先加载
  quality={90}            // 图片质量
/>
```

## 部署注意事项

### Docker 部署

图片会自动包含在 Docker 镜像中：

```dockerfile
# Dockerfile 已配置
COPY --from=builder /app/public ./public
```

### 生产环境

确保图片已优化：
- 使用压缩工具减小文件大小
- 检查图片尺寸是否合适
- 测试不同网络环境下的加载速度

## 故障排查

### 问题 1: 图片不显示

**检查：**
```bash
# 确认图片存在
ls -la frontend/public/backgrounds/

# 应该看到：
# am.jpg
# dr.jpg
# pom.jpg
# wr.jpg
```

**解决：**
- 确保文件名完全匹配（区分大小写）
- 检查文件权限
- 重启开发服务器

### 问题 2: 图片加载慢

**原因：** 图片文件过大

**解决：**
- 使用图片优化工具压缩
- 调整图片尺寸到 1920x1080
- 降低 JPEG 质量到 80-85

### 问题 3: 背景不切换

**检查：**
- 打开浏览器开发者工具
- 查看 Console 是否有错误
- 确认所有图片都能加载

### 问题 4: 内容不清晰

**调整透明度：**
- 增加遮罩层透明度
- 增加内容区背景不透明度
- 调整模糊程度

## 文件结构

```
frontend/
├── public/
│   └── backgrounds/           # 背景图片文件夹
│       ├── README.md         # 图片说明
│       ├── am.jpg            # 背景图片 1
│       ├── dr.jpg            # 背景图片 2
│       ├── pom.jpg           # 背景图片 3
│       └── wr.jpg            # 背景图片 4
├── src/
│   ├── app/
│   │   ├── layout.tsx        # 根布局（引入背景）
│   │   └── globals.css       # 全局样式（优化）
│   └── components/
│       ├── BackgroundSlider.tsx    # 背景轮播组件
│       ├── BackgroundSlider.css    # 背景样式
│       └── MainLayout.tsx          # 主布局（半透明优化）
```

## 相关文档

- [项目文档](../README.md)
- [前端开发指南](./frontend/README.md)
- [部署指南](../DEPLOYMENT.md)

## 效果预览

启动项目后你会看到：
1. 🖼️ 动态背景轮播
2. 🌫️ 毛玻璃效果的界面
3. ✨ 平滑的动画过渡
4. 📱 响应式设计

祝你使用愉快！🚀
